server:
  port: 8080
  servlet:
    application-display-name: option

spring:
  datasource:
    url: jdbc:mysql://db:3306/myopsntion
    username: admin
    password: tlgus1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
      hibernate.format-sql: true
      hibernate.show-sql: true

jwt:
  secret:
    salt: ty
    expire-day: 1d
    header-auth: token
